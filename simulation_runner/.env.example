# simulation_runner Local Development Configuration
# Copy to .env and customize for local development
#
# Configuration Loading Priority:
# 1. Environment variables (highest priority)
# 2. .env file (local development)
# 3. AWS Systems Manager Parameter Store (production)
# 4. Built-in defaults (lowest priority)

# ============================================================================
# FRED Configuration (Local Only - NOT in Parameter Store)
# ============================================================================
# These variables are specific to the local development environment and
# should not be stored in AWS Parameter Store

# Path to FRED framework installation (REQUIRED)
FRED_HOME=/workspaces/fred_simulations/fred-framework

# Workspace directory for simulation downloads and outputs
# Default: /workspace/job_{job_id}
WORKSPACE_DIR=/workspace

# ============================================================================
# Database Configuration
# ============================================================================
# Option 1: Full connection string (recommended)
DATABASE_URL=postgresql://epistemix_user:epistemix_password@localhost:5432/epistemix_db

# Option 2: Individual parameters (bootstrap will construct DATABASE_URL)
# Uncomment these if you prefer individual parameters:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=epistemix_db
# DB_USER=epistemix_user
# DB_PASSWORD=epistemix_password

# ============================================================================
# Epistemix API Configuration
# ============================================================================
# Epistemix API endpoint URL
EPISTEMIX_API_URL=http://localhost:5000

# S3 bucket for job uploads
EPISTEMIX_S3_BUCKET=epistemix-uploads-dev

# ============================================================================
# AWS Configuration
# ============================================================================
# AWS region for S3 and Parameter Store
AWS_REGION=us-east-1

# AWS credentials (for local development)
# In production, use IAM roles instead
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key

# ============================================================================
# Environment
# ============================================================================
# Environment name (dev, staging, production)
# Used to determine if Parameter Store should be queried
ENVIRONMENT=dev

# Application name (used for Parameter Store path prefix)
# Default: epistemix_platform
APPLICATION_NAME=epistemix_platform
