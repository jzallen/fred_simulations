# Epistemix CLI Configuration
# Copy this file to ~/.epistemix/cli.env and customize as needed

# Default environment (TESTING, DEVELOPMENT, PRODUCTION)
EPISTEMIX_ENV=PRODUCTION

# API endpoint URL (used by simulation-runner to download job uploads)
# EPISTEMIX_API_URL=http://localhost:5000

# S3 bucket name (if not specified, will be inferred from AWS config)
# EPISTEMIX_S3_BUCKET=epistemix-uploads-dev

# AWS region (if not specified, will use AWS default)
# AWS_REGION=us-east-1

# Database URL for local storage
DATABASE_URL=sqlite:///epistemix_jobs.db

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Simulation Runner Docker Container Configuration
# =============================================================================
# The simulation-runner container uses these environment variables to connect
# to the Epistemix API and download job uploads before running FRED simulations.
#
# Required environment variables when running simulation-runner:
#   - EPISTEMIX_API_URL: API endpoint for downloading job uploads
#   - AWS credentials: For S3 access (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
#                     or use IAM roles in production
#
# Optional environment variables:
#   - EPISTEMIX_S3_BUCKET: Override default S3 bucket
#   - AWS_REGION: Specify AWS region
#   - DATABASE_URL: Database connection (if API access requires it)
#
# Example docker run command:
#   docker run \
#     -e EPISTEMIX_API_URL=http://host.docker.internal:5000 \
#     -e AWS_REGION=us-east-1 \
#     -e AWS_ACCESS_KEY_ID=your_key_id \
#     -e AWS_SECRET_ACCESS_KEY=your_secret_key \
#     simulation-runner:latest 123
#
# In production with IAM roles (EKS/ECS):
#   - Use IRSA (IAM Roles for Service Accounts) in EKS
#   - Use task IAM roles in ECS
#   - No need to pass AWS credentials explicitly
